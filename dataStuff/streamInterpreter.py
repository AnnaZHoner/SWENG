streamFile = "sampleStreamFile.txt"

# Used to read the oldest line of the file (which is the last one.)
readFile = open(streamFile, "r")
# Used to delete a line once its read.
writeFile = open(streamFile, "w")


while true:
    readfile.read
    
    
    
#writeFile.flush()